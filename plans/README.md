# Implementation Plans

Generated by the `improve` skill on 2026-06-10 against commit `31c1f16` on `main`.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row in this table when done.

## Execution order & status

| Plan | Title                                                                        | Priority | Effort | Depends on | Status                                                                                                |
| ---- | ---------------------------------------------------------------------------- | -------- | ------ | ---------- | ----------------------------------------------------------------------------------------------------- |
| 001  | Add `typecheck` script and CI workflow (typecheck only)                      | P1       | S      | —          | DONE — commit `f238697` in worktree `agent-aac599ae18a3bcb30`, awaiting operator merge                |
| 013  | Switch per-app ESLint configs to flat-config imports + fix `libs/*` ignores  | P1       | S      | —          | BLOCKED — superseded by 014                                                                           |
| 014  | Rebuild ESLint config (design/spike) and clear accumulated lint violations   | P2       | M–L    | —          | TODO (design plan; produces 014a + 014b)                                                              |
| 015  | Repair Jest test suite (design/spike) so CI can gate on tests                | P2       | M–L    | —          | TODO (design plan; produces 015a + 015b)                                                              |
| 016  | Fix `/api/podcast-og` 500 — normalize relative `company_logo` URLs           | P2       | S      | —          | DONE — commit `162bfda` on branch `advisor/016-podcast-og-absolute-urls`, awaiting operator merge     |
| 002  | Upgrade Next.js 16.1.7 → 16.2.9 (Middleware-bypass CVEs)                     | P1       | M      | 001        | DONE — commit `f63f0d5` in worktree `agent-a35a5a0a3671f4601`, awaiting operator merge                |
| 003  | Upgrade Vite 7.1.9 → 7.3.5 (dev-server CVEs)                                 | P2       | S      | —          | DONE — commit `b90dcad` in worktree `agent-ae66a62e21ee0b850`, awaiting operator merge                |
| 004  | Replace `@nrwl/cli@15.9.3` with current Nx CLI                               | P2       | S      | —          | DONE — branch `advisor/004-drop-nrwl-cli`, awaiting operator merge                                    |
| 005  | Unify Prismic loader error returns; delete unused `getPrevNextGlossaryTerms` | P1       | S      | —          | DONE — commit `61b8993` in worktree `agent-a61b2860a1241ce57`, awaiting operator merge                |
| 006  | Cap query-param length in Edge OG handlers                                   | P1       | S      | —          | DONE — commit `0349aec` on branch `advisor/006-cap-og-query-params`, awaiting operator merge          |
| 007  | Fix homepage `setInterval(1000ms)` causing whole-page re-renders             | P1       | S      | —          | DONE — commit `3874a8a` on branch `advisor/007-isolate-homepage-time-widget`, awaiting operator merge |
| 008  | Inline Inter font in OG handlers (remove jsdelivr fetch)                     | P2       | S      | —          | DONE — commit `9ac7b85` on branch `advisor/008-inline-og-inter-font`, awaiting operator merge         |
| 009  | Replace lone lodash barrel import in `banner-preview`                        | P3       | S      | —          | TODO                                                                                                  |
| 010  | Merge 5 JSON-LD `<script>` blocks into a single `@graph`                     | P3       | S      | —          | DONE — merged to `main` via #320                                                                      |
| 011  | Delete dead pages: `welcome.tsx` (Kid-GO) and `live.tsx` (placeholder ID)    | P3       | S      | —          | DONE — branch `advisor/011-delete-dead-pages`, awaiting operator merge                                |
| 012  | Add `.env.example` + initial `CLAUDE.md` + remove root cruft                 | P2       | S      | —          | TODO                                                                                                  |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line reason) | `REJECTED` (with one-line rationale).

## Dependency notes

- **001 no longer depends on 013** (as of 2026-06-12 PM): the operator chose to ship CI gating on **typecheck + test only**, deferring lint to plan 014. This unblocks plan 001 immediately. Plan 013 is superseded.
- **002 depends on 001** because the Next.js upgrade benefits from CI running tests/typecheck on the upgrade PR. If 001 isn't done first, run `yarn test` and `tsc --noEmit` manually after the upgrade and document the result in the PR.
- **014 and 015 are fully independent** and do not block any other plan. Run when the operator has bandwidth for the ESLint and Jest cleanups.
- All other plans are independent and can run in parallel branches.

## Execution log

- 2026-06-10: plan 001 executed in worktree `agent-a9a8dad8a0fac822f` (branch `worktree-agent-a9a8dad8a0fac822f`). STOPPED on pre-existing lint failure. Executor diff is scope-clean and correct (Option A: per-project `typecheck` Nx targets + `yarn typecheck` script + `.github/workflows/ci.yml`). Plan moved to BLOCKED pending 013. Diff can be re-used once 013 lands — re-execute 001 with `improve execute 001`.
- 2026-06-12: plan 013 (original) executed in worktree `agent-a57829b2f56a997c8`. STOPPED — Step 2's `@eslint/eslintrc` resolution pin (3.3.0 → 3.3.5) reproduces the crash; the dep upgrade is not the fix. Executor's diagnostic surfaced the real root cause: `apps/web/eslint.config.js` and `apps/banner-preview/eslint.config.js` wrap `eslint-config-next@16.2.1` (a flat config) in `FlatCompat.extends('next', 'next/core-web-vitals')`, which is the wrong API. Worktree auto-cleaned (no commits). Plan 013 rewritten 2026-06-12 with the correct fix (direct flat-config imports) + a co-located fix for `libs/ui`/`libs/utils` `ignores: ['!**/*']` which surfaces a separate lint failure ("All files matching the following patterns are ignored").
- 2026-06-12 (refined v2) executed in worktree `agent-a5e6c83dd800f421d`. STOPPED — Steps 2–4 fixed the circular-structure crash, but per-app `ignores: ['!**/*']` removal alone did not unblock the libs because root `eslint.config.js:18` had `{ ignores: ['**/*'] }` that propagated into every per-app via `...rootConfig`. Plan 013 broadened to include the root config and re-refined.
- 2026-06-12 (refined v3) executed in worktree `agent-ad65301a69320a4aa`. STOPPED — Steps 2–4b succeeded, but Nx's project-graph validation surfaced a third layer: root config references `@typescript-eslint/no-extra-semi` (removed in `@typescript-eslint@8.x`). Plan 013 refined once more to include deletion of the two dead `no-extra-semi` rule blocks.
- 2026-06-12 (refined v4) executed in worktree `agent-a9424be66c382b87f`. STOPPED on layer 4 — Steps 2–4c succeeded. `libs/ui` and `libs/utils` now produce real `file:line` lint errors (perfectionist/sort-objects) — config-side success for those projects. **But `apps/web` and `apps/banner-preview` hit a fourth layer**: `Config (unnamed): Key "plugins": Cannot redefine plugin "@typescript-eslint"`. Both `eslint-config-next@16.2.1` (via its `typescript-eslint` flat-config bundle) and `plugin:@nx/typescript` (via `FlatCompat`) register `@typescript-eslint` — ESLint v9 forbids duplicate plugin keys across configs.
- 2026-06-12 PM: **strategic pivot #1.** Operator chose to ship CI on typecheck + test only and defer the lint cleanup. Plan 013 marked BLOCKED (superseded by 014). Plan 001 refined to drop the `Lint` step from `.github/workflows/ci.yml` and dropped its dependency on 013. Plan 014 added as the deferred ESLint rewrite + cleanup follow-up (design/spike — produces 014a + 014b). Plans 002–012 unblocked. The four abandoned worktrees from the 013 attempts can be discarded.
- 2026-06-12 PM: plan 001 (refined v2, typecheck+test) executed in worktree `agent-aaf8c3edf874de292`. STOPPED — typecheck is green, but `yarn test` reveals 11 pre-existing failing suites (spec rot since the Jest 30 / React 19 upgrade: unmocked `useRouter`, default-export drift, missing required props, ts-jest strict-index-signature errors). Failing spec files were last touched in commit `8f8f6cc`, well before the plan baseline. Confirmed pre-existing.
- 2026-06-12 PM: **strategic pivot #2.** Operator chose to ship CI on typecheck only and defer the test cleanup. Plan 001 refined to drop the `Test` step from `ci.yml`. Plan 015 added as the deferred Jest repair follow-up (design/spike — produces 015a + 015b). Plans 002–012 remain unblocked. Worktree `agent-aaf8c3edf874de292` can be discarded; the executor's typecheck-only work is captured in plan 001 v3 ready for re-execution.
- 2026-06-12 PM: plan 001 (refined v3, typecheck-only) executed in worktree `agent-aac599ae18a3bcb30` (branch `worktree-agent-aac599ae18a3bcb30` — deviation: the planned `advisor/001-ci-typecheck-baseline` was claimed by an earlier stopped worktree). Commit `f238697`. **APPROVED.** All five done criteria verified independently. Merged to `main` via fast-forward.
- 2026-06-12 PM: plan 002 executed in worktree `agent-a35a5a0a3671f4601` (branch `worktree-agent-a35a5a0a3671f4601`). Commit `f63f0d5`. **APPROVED.** Next.js bumped to `^16.2.9` (latest stable 16.2.x), `eslint-config-next` aligned. `yarn npm audit` confirms both Middleware-bypass CVEs are closed. Typecheck + build pass. Smoke tests: 3/4 URLs return 200; `/api/podcast-og?title=Hello&episode=1` returns 500 — executor verified pre-existing on baseline `f238697`. Documented deviation accepted on merit; 500 is unrelated to the upgrade. Pre-existing podcast-og bug became plan 016. Merged to `main`.
- 2026-06-12 PM: plan 003 executed in worktree `agent-ae66a62e21ee0b850` (branch `worktree-agent-ae66a62e21ee0b850`). Commit `b90dcad`. **APPROVED.** Vite bumped to `^7.3.2` (resolves to 7.3.5). Deviation accepted on merit: plan asked for `^7.4` but Vite's 7.x line ends at 7.3.5 (no 7.4 release exists; 8.x major jump is out of scope). CVE advisories list `Vulnerable Versions <=7.3.1`, so 7.3.2 is the patch release. `yarn npm audit` confirms both `server.fs.deny` and dev-server-WebSocket CVEs are closed. Typecheck + lib build pass. Merged to `main`.
- 2026-06-12 PM: plan 005 (v1) executed in worktree `agent-a133b1f8986fc623f`. STOPPED — removing the loose `return error` path narrowed `clients` in callers, and `apps/web/pages/about.tsx:35-39`'s `satisfies GetStaticProps<{ clients: Customer[] }>` clause couldn't absorb the now-strict `PrismicDocument[]` type. Plan refined to add a `Customer[]` cast on the success path of both `getAllClients` and `getVisibleClients` (matching `feedback.ts:36`'s existing pattern).
- 2026-06-12 PM: plan 005 (v2, refined) executed in worktree `agent-a61b2860a1241ce57` (branch `advisor/005-unify-prismic-error-returns`). Commit `61b8993`. **APPROVED.** All four loaders now return `{ key: [] }` on error; `client.ts` casts the success path to `Customer[]`. `getPrevNextGlossaryTerms` deleted (-25 lines). `yarn typecheck` + `yarn build` pass. Awaiting operator merge.
- 2026-06-14: plan 007 executed in worktree `agent-a01e8a2d6c700008c` (branch `advisor/007-isolate-homepage-time-widget`). Commit `3874a8a`. **APPROVED.** Single-file change (+6/-2 in `apps/web/pages/index.tsx`): new `LocalTimeInFrance` leaf component inserted between `useGetLocalTimeInFrance` hook and `ProblemAgitation`; `ProblemAgitation` no longer calls the hook directly; JSX uses `<LocalTimeInFrance />`. `yarn typecheck` + `yarn build` pass. Awaiting operator merge.
- 2026-06-22: plan 016 executed on branch `advisor/016-podcast-og-absolute-urls`. Commit `162bfda`. Single-file change (+10/-2 in `apps/web/pages/api/podcast-og.tsx`): added `toAbsoluteUrl` helper that derives `origin` from `req.url`; applied to `currentEpisodeLogo` and `randomLogos`. Smoke tests on `?slug=skia` and `?title=Hello&episode=1` both return `200 image/png` (PNG 1200x630). `yarn typecheck` + `yarn build` pass. Awaiting operator merge.
- 2026-06-22: plan 006 executed on branch `advisor/006-cap-og-query-params`. Commit `0349aec`. Two-file change (+19/-5 in `apps/web/pages/api/og.tsx` and `apps/web/pages/api/podcast-og.tsx`): added `clampParam` helper + `MAX_TITLE_LEN`/`MAX_GUEST_LEN`/`MAX_SLUG_LEN` constants to each handler; applied caps at every consumption site. `/api/og` smoke-tested with 5KB title — returns `200 image/png` (silently truncated). `/api/podcast-og` smoke verification deferred to the 016 branch which fixes the pre-existing 500 on that endpoint; caps themselves are in place. `yarn typecheck` + `yarn build` pass. Merged to `main` via #317.
- 2026-06-22: plan 008 executed on branch `advisor/008-inline-og-inter-font`. Commit `9ac7b85`. Three-file change (+2/-8 plus new `apps/web/public/fonts/inter-latin-700-normal.woff` ~31KB): vendored the Inter 700 WOFF and rewrote both handlers' font `fetch` to use `new URL('../../public/fonts/inter-latin-700-normal.woff', import.meta.url)`. Smoke tests on `/api/og?title=Hello` and `/api/podcast-og?title=Hello&episode=1` return `200 image/png` (PNG 1200x630). `yarn typecheck` + `yarn build` pass. Awaiting operator merge.

## Findings considered and rejected

- **Multiple internal `package.json@0.0.1`** — only `libs/utils/package.json` exists; not a pattern.
- **`dangerouslySetInnerHTML` on JSON-LD scripts as injection risk** — content is hard-coded literals, no user/CMS data. By-design.
- **`fs.readFileSync(slug)` path-traversal in podcast pages** — gated by `getStaticPaths` with `fallback: false`. Re-audit if fallback ever changes.
- **Avatar `onError` DOM manipulation as XSS** — `textContent` is safe; email is hardcoded team data.
- **`next.config.js` `dangerouslyAllowSVG: true` as XSS** — paired with strict CSP for next/image. Standard mitigation in place.
- **Lodash CVE GHSA-r5fr-rjxr-66jc** — not surfaced by current `yarn npm audit`; subagent overreported.
- **Fixtures ship to client bundle** — Next.js tree-shakes imports only used in `getStaticProps`/`getStaticPaths`. Most pages qualify. Edge handler import (`podcast-og.tsx`) is the real exception and would be addressed by a future redesign of #10 in the audit table.

## Findings not selected this round

These were in the audit but not picked for plan-writing. Re-run the skill with `plan <description>` if any becomes priority.

- **#9 transcript parser characterization tests** (M effort, blocked by need to define golden fixtures)
- **#10 podcast-og collision algorithm rewrite** (M effort, MED risk to OG visual output)
- **#12 type Prismic schemas** (S effort but couples to a wider Prismic typegen decision)
- **#13 move data loaders out of `pages/api/`** (M effort, touches many imports)
- **#15 glossary `getAllByType` N+1 caching** (S effort, but build-time only; low urgency)
- **#17 TailwindCSS v3 → v4** (M effort, design system risk)
- **#18 split Pricing component** (M effort, no test net for safe refactor — needs 001 + char tests first)
- **#19 bonjour/index dedup via i18n** (L effort, fragile)
- **Direction findings D-A through D-E** (design/spike plans, separate decision)
