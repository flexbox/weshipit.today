# Implementation Plans

Generated by `/improve davidl-fr` on 2026-06-28. Each plan is fully self-contained — read it end-to-end before starting, honor the STOP conditions, and update your row in the status table when done.

## Execution order & status

| Plan | Title                                                            | Priority | Effort | Depends on | Status                                                                                                                                                  |
| ---- | ---------------------------------------------------------------- | -------- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001  | davidl-fr quick-wins janitor pass                                | P1       | S      | —          | DONE — PR #325                                                                                                                                          |
| 002  | Maestro testing page — Service/Breadcrumb/Review structured data | P1       | S      | —          | DONE — merged to `main` as `8454ad0`                                                                                                                    |
| 003  | Maestro testing page — title/meta/OG-alt optimization            | P1       | S      | —          | DONE — branch `advisor/003-maestro-testing-title-meta-og` @ `70af1f2` (rebased on `main`, not yet merged)                                               |
| 004  | Maestro testing page — contextual internal links (next/link)     | P2       | S      | —          | DONE — branch `advisor/004-maestro-testing-internal-links` @ `037defd` (uses next/link; supersedes reverted `da0db9b`; cut from `main`, not yet merged) |
| 005  | Tools index — "React Native" keyword in H1                       | P1       | S      | —          | DONE — branch `advisor/005-tools-index-h1-keyword` @ `94d6aa4` (cut from `main` @ `2ff7d83`, not yet merged)                                            |
| 006  | Tool detail pages — strip markdown from meta descriptions/schema | P1       | S      | —          | DONE — branch `advisor/006-tool-detail-meta-descriptions` @ `c083912` (cut from `2ff7d83`, merges cleanly with 005; not yet merged)                     |
| 007  | Tool detail pages — same-type recommendations + heading fix      | P2       | S      | 006        | DONE — branch `advisor/007-tools-related-by-type` @ `2bc60d7` (stacked on 006 @ `c083912`; merging 007 brings both; not yet merged)                     |
| 008  | Tools crawl hygiene — ItemList URLs, `?id=` param, sitemap       | P2       | S      | —          | DONE — branch `advisor/008-tools-crawl-hygiene` @ `cf965a5` (cut from `2ff7d83`, merges cleanly with 005/006; not yet merged)                           |

> Plans 002–004 generated by `/improve the seo for the testing page with maestro` on 2026-07-02, against commit `99172de`. Target file: `apps/web/pages/react-native-testing.tsx`.
>
> Plans 005–008 generated by `/improve seo for https://weshipit.today/react-native-tools` on 2026-07-08, against commit `a7457e8`. Target files: `apps/web/pages/react-native-tools/{index,[slug]}.tsx`, `libs/ui/src/lib/tool-list/tool-card.tsx`, `apps/web/project.json`.

## Dependency notes

- `001` is a bundle of 8 independent S/LOW-risk fixes (davidl-fr).
- `002`, `003`, `004` are mutually independent but all edit
  `apps/web/pages/react-native-testing.tsx` in **different regions** (JSON-LD
  consts / `Layout` SEO props / `WhoAmI` prose). Recommended order 002 → 003 →
  004 to keep diffs clean; they can also be done together in one branch.
- Verification note for 002–008: gate on `npx nx typecheck web` and
  `npx nx build web`. **Do not** gate on `npx nx lint web` — it fails for a
  pre-existing monorepo eslint-config error unrelated to these changes.
- `005`–`008` are mutually independent. `006` and `007` both edit
  `apps/web/pages/react-native-tools/[slug].tsx` in **different regions**
  (`getStaticProps` seoDescription/schema vs `recommendedRecords`/heading) —
  recommended order 006 → 007 to keep diffs clean. `005` and `008` both touch
  `apps/web/pages/react-native-tools/index.tsx` in different regions (Hero
  `title` vs ItemList schema). `008` also edits the shared
  `libs/ui/src/lib/tool-list/tool-card.tsx` — its Step 2 usage sweep must pass
  before changing it.

## Findings considered and rejected (this audit)

These were evaluated and intentionally not turned into plans this round:

- **`@next/mdx` + `next-mdx-remote` both in deps** — looked redundant; in fact they serve different purposes (`@next/mdx` handles `.mdx` page files via webpack; `next-mdx-remote` serializes/renders remote MDX fetched in `getStaticProps`). Both are actively used. Keep.
- **`prism-themes` + `prismjs` bundle duplication suspicion** — MED-confidence performance finding; would need bundle-analyzer evidence before sizing a plan.
- **Magic numbers in `workshop.ts countChallenges` (`count -= 2`)** — folded into plan 001's stale-test sub-task; not worth a standalone plan.
- **`findNextChallenge.ts` uses `.map` for side-effects** — micro; same as above.
- **Pre-existing monorepo lint regression** (`nx lint web` and `nx lint davidl-fr` both fail with circular-structure error in `FlatCompat` resolving `eslint-plugin-react@7.37.4`) — affects `apps/web` too, not davidl-fr-specific. Out of scope for this audit; track separately.

### SEO audit of `react-native-testing.tsx` (2026-07-02)

- **Rewrite the H1 to carry the primary keyword** — the hero H1 ("Most React Native teams skip e2e tests. Until the first production crash.") is a conversion-first emotional hook with no hard keyword. NOT planned: rewriting it trades proven conversion copy for a marginal SEO gain, and the sub-hero paragraph already states the product ("Maestro end-to-end tests for your React Native app"). Better handled as a future A/B test than a mechanical edit. Re-surface only with conversion data.

### SEO audit of `/react-native-tools` (2026-07-08, commit `a7457e8`)

Audited but **not planned** this round:

- **SoftwareApplication schema missing `offers`/`aggregateRating`** (`[slug].tsx:52-60`) — schema is valid but ineligible for rich results; an `offers` block could be derived from the fixture `pricing` field (Free tier → price 0). Worth a small follow-up after 006 lands (same `getStaticProps` region).
- **Tools index is a pure card grid — thin for the "react native tools" head term** — no FAQ, no category explanations, sponsor link is the only intro link. M-effort content work; re-surface if the page becomes a deliberate traffic asset.
- **`?type=` filter URLs as duplicate content** — REJECTED: raw static-HTML canonical is already the clean base URL; only the JS-rendered canonical drifts, and plan 008 removes the worse `?id=` variant of the same problem at the source.
- **Client-only search bar** — REJECTED: correct as-is, no SEO surface.

## Findings selected but not yet planned

These were rated worth doing but not selected for plans in this round. Re-surface in a future `/improve` pass or request directly:

- **Providers SSR-flash via `visibility:hidden`** (`src/components/Providers/Providers.tsx:70`)
- **API security hardening** (newsletter + login error responses, method guards, email validation)
- **`magic-sdk` extracted from global** — every non-workshop page currently ships the SDK and triggers an auth check on mount
- **`react-gravatar` → React 19 compatible replacement** (4 components affected; chips ~16 TS errors off the 281-error backlog)
- **Full typecheck cleanup + re-enable CI** (281 TS errors; CI exclusion added in `.github/workflows/ci.yml:33` during the migration)
- **`/api/login` server session + cookie** — current auth is purely client-side
- **`styled.img` → `next/image`** in blog/testimonials/mindmaps/design-details
- **Test coverage backfill** — 0 component tests, 1 partial page test
- **DIR-1 brand consolidation spike** — `davidl-fr` (50 pages) overlaps with `weshipit.today` service funnel
- **DIR-2 Prismic → in-repo MDX spike** — partial migration already in flight; both stacks present

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)
