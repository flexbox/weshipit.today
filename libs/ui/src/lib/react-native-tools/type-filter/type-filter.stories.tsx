import type { Meta } from '@storybook/react';
import { TypeFilter } from './type-filter';

const Story: Meta<typeof TypeFilter> = {
  component: TypeFilter,
  title: 'TypeFilter',
};
export default Story;

export const Primary = {
  args: {},
};
