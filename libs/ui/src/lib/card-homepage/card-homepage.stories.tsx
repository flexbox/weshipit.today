import type { Meta } from '@storybook/react';
import { CardHomepage } from './card-homepage';

const Story: Meta<typeof CardHomepage> = {
  component: CardHomepage,
  title: 'CardHomepage',
};
export default Story;

export const Primary = {
  args: {},
};
