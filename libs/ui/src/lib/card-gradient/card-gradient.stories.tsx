import type { Meta } from '@storybook/react';
import { CardGradient } from './card-gradient';

const Story: Meta<typeof CardGradient> = {
  component: CardGradient,
  title: 'CardGradient',
};
export default Story;

export const Primary = {
  args: {},
};
